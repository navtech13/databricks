import { useState, useEffect } from "react"
import { Meta, Story, Props, Preview } from "@storybook/addon-docs"
import ThumbnailModal from "."

<Meta
  title='COMPONENTS/Modal/Thumbnail Modal'
  component={ThumbnailModal}
  argTypes={{
    videoSrc: {
      name: "Video source (delete to show image on modal)",
      control: { type: "text" },
    },
    image: {
      name: "Image source",
      control: { type: "text" },
    },
    className: {
      table: { disable: true },
    },
    imageOptions: {
      table: { disable: true },
    },
    rootElement: {
      table: { disable: true },
    },
  }}
/>

# ThumbnailModal

A component that shows a thumbnail, which can be clicked to display either:

- The full image
- An video (must be embed ready)

## Props

<Props of={ThumbnailModal} />

### ThumbnailModal

<Preview>
  <Story
    name='Thumbnail Modal'
    args={{ videoSrc: "https://www.youtube.com/embed/PftRBoqjhZM" }}
  >
    {(args) => {
      return (
        <div className='max-w-xs'>
          <ThumbnailModal
            src={
              args.image ||
              "static/images/2021-02-Databricks-Gartner-OG-1200x628-1.jpg"
            }
            alt='Alt'
            videoSrc={args.videoSrc}
          />
        </div>
      )
    }}
  </Story>
</Preview>
