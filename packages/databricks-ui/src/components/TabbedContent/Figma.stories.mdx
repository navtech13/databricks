import { Meta, Story, Props, Preview } from "@storybook/addon-docs"
import TabbedContent from "."
import { tabData } from "./tabData.js"

<Meta
  title='COMPONENTS/Tabs/Pill Tabs'
  component={TabbedContent}
  argTypes={{
    children: {
      table: { disable: true },
    },
    tabData: {
      table: { disable: true },
    },
    tabs: {
      name: "Tabs amount",
      control: { type: "select" },
      options: [1, 2, 3, 4, 5, 6, 7],
    },
  }}
/>

# Tabbed Content

The component allows for any type of module on the web to be placed inside a tabbed selector so that the user can toggle the content they are viewing. It gives us the ability to condense the overall length of a page by letting multiple pieces of content fit within the same screen real estate.

## Props

<Props of={TabbedContent} />

<Preview>
  <Story name='Pill Tabs'>
    {(args) => {
      return (
        <TabbedContent
          eyebrow={args.eyebrow || "Features"}
          title={
            args.title || "<p>End-to-end data.</p><p>Top-to-bottom use cases.</p>"
          }
          headlineTag={args.headlineTag || "h4"}
          tabData={tabData?.slice(0, args.tabs)}
          variant={args.variant}
          tabAlignment={args.tabAlignment || "left"}
        />
      )
    }}
  </Story>
</Preview>
