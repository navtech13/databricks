import { Meta, Story, Props, Preview } from "@storybook/addon-docs"
import TwoColumnsContent from "."
import BaseLayout from "../BaseLayout"
import { DemoFooter, DemoNav } from "../BaseLayout/BaseLayoutData"

<Meta title='Pages/TwoColumns' component={TwoColumnsContent} />

# TwoColumnsContent

Displays the data into two columns, stacking vertically on mobile viewports and horizontally on larger viewports.

## Props

<Props of={TwoColumnsContent} />

### TwoColumnsContent

<Preview>
  <Story name='TwoColumns'>
    <BaseLayout
      variant='minimal'
      footerComponent={<DemoFooter className=' bg-navy-05 min-h-inherit' />}
    >
      <TwoColumnsContent
        firstColumn={
          <div className='h-[700px]'>
            <h2>First column</h2>
          </div>
        }
        secondColumn={
          <div className='h-[700px]'>
            <h2>Second column</h2>
          </div>
        }
      />
    </BaseLayout>
  </Story>
</Preview>
