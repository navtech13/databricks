import { useState, useEffect, Fragment } from "react"
import { Meta, Story, Props, Preview } from "@storybook/addon-docs"
import Slider from "."
import Wrapper from "../Wrapper"
import cardSliderData from "./cardSliderData"
import imageSliderData from "./imageSliderData"

<Meta
  title='COMPONENTS/Carousel | Slider/Card Slider'
  component={Slider}
  args={{ autoplayDelay: 3000 }}
  argTypes={{
    items: {
      table: { disable: true },
    },
    controls: {
      table: { disable: true },
    },
    pagination: {
      table: { disable: true },
    },
    playLabel: {
      table: { disable: true },
    },
    pauseLabel: {
      table: { disable: true },
    },
    nextLabel: {
      table: { disable: true },
    },
    prevLabel: {
      table: { disable: true },
    },
    columns: {
      table: { disable: true },
    },
  }}
/>

# Slider

Basic slider component

## Props

<Props of={Slider} />

### 7.6 Card Slider

<Preview>
  <Story name='Card Slider'>
    {(args) => {
      return (
        <Wrapper>
          <Slider items={cardSliderData} {...args} />
        </Wrapper>
      )
    }}
  </Story>
</Preview>