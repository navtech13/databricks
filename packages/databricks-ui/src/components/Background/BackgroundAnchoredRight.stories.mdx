import { Preview, Meta, Story, Props } from "@storybook/addon-docs"
import Background from "."

<Meta
  title='Pages/Background/Background Anchored Right'
  component={Background}
  argTypes={{
    variant: {
      table: { disable: true },
    },
    children: {
      table: { disable: true },
    },
  }}
/>

export const BackgroundTemplate = (args) => <Background {...args} />

## Background

Background component provides a background image. It accepts one optional prop: "variant" which specify the variant to use for the background.

<Props of={Background} />

<Preview>
  <Story
    name='Background Anchored Right'
    args={{
      variant: "anchored_right",
      children: <div className='h-[500vh]'></div>,
    }}
  >
    {BackgroundTemplate.bind({})}
  </Story>
</Preview>
