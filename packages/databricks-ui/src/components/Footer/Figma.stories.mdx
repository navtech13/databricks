import { useState, useEffect } from "react"
import { Meta, Story, Props, Preview } from "@storybook/addon-docs"
import Footer from "."
import Sidebar from "../Sidebar"
import footerData from "./footerData"

<Meta
  title='COMPONENTS/Navigation/Footer'
  component={Footer}
  argTypes={{
    children: { table: { disable: true } },
  }}
/>

# Footer

Footer component.

## Props

<Props of={Footer} />

### Footer

<Preview>
  <Story name='Footer'>
    <div className='py-30'>
      <Footer>
        <Footer.MainSection>
          <Footer.MenuSection>
            {footerData.menus.map((menu) => {
              return <Footer.Menu key={JSON.stringify(menu)} items={menu} />
            })}
          </Footer.MenuSection>
          {footerData.items && (
            <Footer.Region>
              {footerData.items.map((node, i) => {
                return <React.Fragment key={node.key}>{node}</React.Fragment>
              })}
            </Footer.Region>
          )}
        </Footer.MainSection>
        <Footer.Disclaimer links={footerData.disclaimer.links}>
          {footerData.disclaimer.copyright}
        </Footer.Disclaimer>
      </Footer>
    </div>
  </Story>
</Preview>
